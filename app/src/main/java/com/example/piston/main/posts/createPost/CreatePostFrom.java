package com.example.piston.main.posts.createPost;

public class CreatePostFrom {
    public enum CreateFromFolder {users, folders, posts}
    public enum CreateFromGroup {groups, posts}
    public enum CreateFromCategory {categories, posts}
}
